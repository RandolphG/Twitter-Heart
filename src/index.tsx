import React from "react";
import ReactDOM from "react-dom";
import Heart from "./App";

ReactDOM.render(
  <React.StrictMode>
    <Heart />
  </React.StrictMode>,
  document.getElementById("root")
);
